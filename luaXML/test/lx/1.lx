print("inicio do test")

local function soma(props, childrens)
    local soma = 0 
    for _, valor in ipairs(childrens) do
        soma = soma + valor
    end

    return soma + props.t
end

local pt = function(props, childrens) print(table.unpack(childrens)) end


<pt>
    <soma>
        {1}{2}{3}
    <soma/>
</pt>

print("final do test")
